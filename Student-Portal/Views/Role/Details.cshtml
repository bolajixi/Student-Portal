@model Student_Portal.Models.RoleViewModel

@{
    ViewBag.Title = "Role";
}

    <div class="row justify-content-md-center">
        <div class="col-lg-9 mt-5">
            <div class="card">
                <div class="card-body">
                    <div>
                        <dl class="dl-horizontal">
                            <dt>
                                Role Name:
                            </dt>

                            <dd>
                                @Html.DisplayFor(model => model.Name, new { @class = "pull-right" })
                            </dd>

                        </dl>
                    </div>
                    <p>
                        @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
                        @Html.ActionLink("Back to List", "Index")
                    </p>
                </div>
            </div>
        </div>
    </div>
